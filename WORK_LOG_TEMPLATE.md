# BUILD & LEARN 공식 작업 로그 템플릿

이 문서는 Codex 작업이 끝날 때마다 실제 사이트의 `src/content/logs/`에 작업 로그를 추가하기 위한 기준입니다. 어려운 개발 용어를 나열하기보다 나중에 다시 읽었을 때 **무슨 문제가 있었고, 왜 그렇게 고쳤으며, 무엇을 확인했는지** 이해할 수 있게 작성합니다.

## 작성 원칙

- 실제로 한 일만 작성하고 경험이나 결과를 지어내지 않습니다.
- 레이아웃 확인만을 위한 placeholder 글은 사이트 Log에 추가하지 않습니다.
- “수정함”에서 끝내지 않고 작업 전 상태와 작업 후 차이를 설명합니다.
- 추가·수정·삭제가 없더라도 `없음`이라고 적어 작업 범위를 분명히 합니다.
- 명령 실행과 브라우저 확인을 구분합니다.
- 체크박스는 실제로 확인한 항목만 `[x]`로 표시합니다.
- 관련 파일은 프로젝트 루트 기준 경로로 적습니다.
- 새로운 Codex 작업 로그에는 `Codex` 태그를 반드시 넣습니다.

## 글 블록 사용법

본문은 기술 블로그처럼 큰 흐름을 먼저 보여주고, Notion처럼 필요한 정보만 블록으로 구분합니다. 한 글에 모든 블록을 억지로 넣지 말고 실제 내용을 더 쉽게 이해시키는 요소만 사용합니다.

### 제목과 부제목

- `##`는 작업의 큰 단계에 사용합니다.
- `###`는 큰 단계 안의 세부 내용에 사용합니다.
- `####`는 짧은 보충 설명에만 사용합니다.
- 페이지 제목은 frontmatter의 `title`이 화면의 `h1`이 되므로 본문에 `#`를 다시 쓰지 않습니다.

### 체크박스

```md
- [x] 실제로 확인한 항목
- [ ] 아직 확인하지 않은 항목
```

### 정보 입력 박스

실제 입력창이 아니라 값이나 조건을 한눈에 보여주는 읽기 전용 정보 상자입니다.

```mdx
<InputBox label="작업 범위">Log 상세 페이지의 본문 표현만 변경</InputBox>
```

### 강조 안내 박스

`tone`은 `note`, `warning`, `success` 중 하나를 사용합니다.

```mdx
<Callout title="초보자 메모" tone="note">
  MDX는 Markdown 안에서 미리 준비한 React 컴포넌트를 사용할 수 있습니다.
</Callout>
```

### 인용과 코드

````md
> 결정의 핵심이나 기억할 문장을 적습니다.

`SITE_URL`처럼 문장 안의 코드는 백틱 한 개를 사용합니다.

\```ts
const ready = true;
\```
````

### 표

```md
| 구분     | 사용 목적             |
| -------- | --------------------- |
| Callout  | 주의·성공·핵심 메모   |
| InputBox | 범위·결과처럼 짧은 값 |
```

## 파일 이름

영문 소문자와 하이픈을 사용합니다.

```text
round-5-deployment-preparation.mdx
search-filter-bug-fix.mdx
```

## 복사용 템플릿

```mdx
---
title: "[N차] 한 줄 작업 제목"
summary: "목록에서 작업 결과를 이해할 수 있는 한 줄 요약"
category: "dev"
tags: ["Codex", "관련 키워드 1", "관련 키워드 2"]
isPublished: true
createdAt: "YYYY-MM-DD"
updatedAt: "YYYY-MM-DD"
---

## 작업 전 상태

작업을 시작했을 때 사이트가 어떤 상태였는지 적습니다.

<InputBox label="한 줄 상태">작업 전 상태를 가장 짧게 요약합니다.</InputBox>

## 요청한 내용

무엇을 왜 바꾸려고 했는지 쉬운 말로 적습니다.

## 문제 원인

문제가 생긴 이유를 확인할 수 있었다면 적습니다. 원인이 없는 단순 기능 추가라면 이 섹션은 생략할 수 있습니다.

## 변경한 내용

### 추가

- 없음

### 수정

- 없음

### 삭제

- 없음

## 왜 이렇게 변경했나

여러 방법 중 현재 방법을 선택한 이유와 포기한 방법이 있다면 그 이유를 적습니다.

> 가장 중요했던 판단을 한 문장으로 적습니다.

## 초보자가 알아둘 점

이번 작업에서 배울 수 있는 개념을 전문 용어부터 쓰지 말고 일상적인 설명부터 적습니다.

<Callout title="초보자 메모" tone="note">
  이번 작업의 핵심 개념을 쉬운 말로 설명합니다.
</Callout>

## 확인한 내용

### 자동 검사

- [ ] `npm run lint`
- [ ] `npm run type-check`
- [ ] `npm run format:check`
- [ ] `npm run build`

### 직접 확인

- [ ] 로컬 서버에서 관련 페이지 확인
- [ ] 모바일 화면 확인
- [ ] 데스크톱 화면 확인
- [ ] 브라우저 콘솔 오류 확인

## 관련 파일

- `src/example.ts`

## 남아 있는 점

현재 작업 범위에서 해결하지 않았거나 실제 정보가 필요해 보류한 내용을 적습니다. 없으면 `현재 작업 범위에서는 없음`이라고 작성합니다.

## 다음에 할 일

다음 작업이 정해졌다면 적고, 없으면 `아직 정해지지 않음`이라고 작성합니다.
```

## 일반 학습 로그에 적용할 때

실제로 학습하거나 실험한 내용만 작성합니다. `기록한 이유 → 직접 확인한 내용 → 이해한 원리 → 남은 질문 → 다음에 할 일` 순서를 권장하며, 실행하지 않은 명령이나 확인하지 않은 결과에는 완료 표시를 하지 않습니다.
