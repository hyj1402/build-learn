# BUILD & LEARN 개발 요청 관리 문서

이 문서는 새로운 개발 요청을 받을 때마다 **같은 파일을 계속 수정하는 단일 작업 요청서**입니다. 완료된 작업의 자세한 기록은 `DEVELOPMENT_LOG.md`에 누적합니다.

## 사용하는 방법

1. 새 요청을 시작할 때 아래의 `현재 작업`을 새 내용으로 교체합니다.
2. 무엇을 추가·수정·삭제하는지 먼저 구분합니다.
3. 작업 전에 현재 문제와 완료 조건을 적습니다.
4. 작업이 끝나면 실제 변경 결과와 검증 명령을 기록합니다.
5. 자세한 과정과 초보자 설명은 `DEVELOPMENT_LOG.md`에 날짜별로 추가합니다.

---

## 현재 작업

### 기본 정보

- 작업 이름: Project와 Log의 MDX 색상 범위 분리
- 시작일: 2026-08-16
- 상태: 완료
- 참고 문서: `BUILD_LEARN_Codex_8차작업지시서.md`
- 작업 원칙: 공용 MDX 구조는 유지하고 Log 전용 다색 CSS만 `.log-detail` 안으로 제한

### 요청 배경

Log 상세을 위해 만든 다색 에디토리얼 CSS가 공용 `.mdx-content`에 적용되어 Project 상세까지 파랑·보라·초록·앰버 색상을 사용하고 있었습니다. Project는 흑백과 주황 한 가지 포인트만 쓰도록 페이지별 스타일 범위를 분리합니다.

### 추가할 것

- [x] 실제 8차 보완 작업 Log

### 수정할 것

- [x] Project 공용 MDX를 흑백·주황 단일 포인트로 복원
- [x] Log 전용 네 가지 구역색을 `.log-detail` 범위로 제한
- [x] Log 인라인 코드 규칙이 코드 블록에 들어가지 않도록 예외 추가

### 삭제할 것

- [x] 없음

### 하지 않을 것

- MDX 컴포넌트 구조와 콘텐츠 변경
- GitHub push와 Vercel 재배포

### 완료 조건

- Project 상세에 Log 전용 다색 배경이 표시되지 않음
- Log 상세에는 기존 네 가지 구역색이 유지됨
- 공용 체크박스·코드·표의 가독성 기능은 유지됨
- `lint`, `type-check`, `format:check`, `build`가 모두 통과함

### 작업 결과

- Project 상세의 제목 배경·왼쪽 테두리를 제거하고 목록 포인트를 주황색으로 복원했습니다.
- Log 상세의 파랑·보라·초록·앰버 제목과 Callout·InputBox 색상은 유지했습니다.
- Home, Projects, Log, About, Contact에 가로 넘침과 브라우저 콘솔 오류가 없었습니다.
- `npm run lint`, `npm run type-check`, `npm run format:check`, `npm run build`가 모두 통과했습니다.

---

## 다음 요청에서 복사해 사용할 형식

```md
## 현재 작업

### 기본 정보

- 작업 이름:
- 시작일:
- 상태: 대기 / 진행 중 / 완료 / 보류
- 참고 문서:
- 작업 원칙:

### 요청 배경

### 추가할 것

- [ ]

### 수정할 것

- [ ]

### 삭제할 것

- [ ]

### 하지 않을 것

-

### 완료 조건

-

### 작업 결과

-
```
