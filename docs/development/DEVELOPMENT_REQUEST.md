# BUILD & LEARN 개발 요청 관리 문서

이 문서는 새로운 개발 요청을 받을 때마다 **같은 파일을 계속 수정하는 단일 작업 요청서**입니다. 완료된 작업의 자세한 기록은 `docs/development/DEVELOPMENT_LOG.md`에 누적합니다.

## 최근 완료 작업

- 작업 이름: 관리자 사이드바·테마 토글·Admin 링크 보안 수정 문서 정합성 정리
- 완료일: 2026-09-17
- 상태: 완료
- 결과: `PROJECT_STATUS.md`가 반영하지 못했던 세 커밋(`0ca021e` 테마 토글 이동, `09a03d6` Admin 링크 권한 체크, `8d18788` 사이드바 스타일)을 상태 문서와 누적 개발 로그에 반영했습니다.
- 다음 작업 시작 시: 이 구역을 요청 배경·범위·완료 조건이 있는 새 요청으로 교체합니다.

> 아래는 2026-09-16에 완료한 문서 정합성 작업(다크 모드·댓글 기능)의 상세 기록입니다.

### 기본 정보

- 작업 이름: 다크 모드·댓글 기능 문서 정합성 정리
- 시작일: 2026-09-16
- 상태: 완료
- 참고 문서: `docs/harness/PROJECT_STATUS.md`, `docs/development/DEVELOPMENT_LOG.md`
- 작업 원칙: 코드를 바꾸지 않고, 이미 구현된 기능이 상태·로그 문서에 정확히 반영되도록만 한다

### 요청 배경

`git log` 확인 결과 `PROJECT_STATUS.md`(2026-09-13 하네스 정리 커밋 `ebe0b04` 기준 작성)가 그 이후 커밋 두 개(`aa3e1aa`, `c822d39`)를 반영하지 못하고 있었습니다. 그 결과 이미 구현된 Log 댓글 기능이 "아직 없음" 목록에 남아 있었고, 다크 모드는 상태 문서에 아예 언급이 없었습니다. `DEVELOPMENT_LOG.md`에도 두 커밋의 작업 기록이 없었습니다.

### 추가할 것

- [x] `DEVELOPMENT_LOG.md`에 두 커밋(About 타임라인·Log 댓글, 다크 모드·댓글 버그 수정)에 대한 누적 로그 항목

### 수정할 것

- [x] `PROJECT_STATUS.md`의 "현재 구현 상태"에 다크 모드 추가
- [x] `PROJECT_STATUS.md`의 "Supabase·관리자 기능" 완료 목록에 About 커리어 타임라인, Log 회원 댓글 추가
- [x] `PROJECT_STATUS.md`의 "아직 없음" 목록에서 댓글 제거
- [x] `PROJECT_STATUS.md`의 "다음 작업 후보" 1번에 댓글·다크 모드 운영 검증 항목 추가

### 삭제할 것

- 없음

### 하지 않을 것

- 애플리케이션 코드 변경
- 새 기능 추가
- Vercel 실제 운영 검증(다음 작업 후보로 남겨둠)

### 완료 조건

- `PROJECT_STATUS.md`가 최신 커밋(`c822d39`)까지의 실제 구현 상태를 정확히 설명함
- `DEVELOPMENT_LOG.md`에 두 커밋에 대응하는 기록이 있음
- `npm run format:check` 통과

### 작업 결과

- `PROJECT_STATUS.md`에 다크 모드, About 커리어 타임라인, Log 회원 댓글을 완료 항목으로 반영하고 "아직 없음" 목록을 정정했습니다.
- `DEVELOPMENT_LOG.md`에 두 커밋에 대응하는 로그 2건(2026-09-13 (15), (16))을 추가했습니다. 다만 두 커밋 자체에는 당시의 lint/type-check/build 실행 기록이나 RLS 허용/거부 실제 테스트 기록이 남아 있지 않아, 이번 로그에는 커밋 diff와 메시지에서 확인 가능한 사실만 적고 그 한계를 명시했습니다.
- 코드는 변경하지 않았습니다. `npm run format:check`만 통과를 확인했습니다(문서만 변경했으므로 `docs/harness/QUALITY_GATES.md`의 "문서만 변경한 경우" 기준 적용).

> 아래의 상세 내용은 2026-09-09에 완료된 하네스 보강 작업의 기록입니다. 활성 개발 요청이 아닙니다.

## 사용하는 방법

1. 새 요청을 시작할 때 아래의 `현재 작업`을 새 내용으로 교체합니다.
2. 무엇을 추가·수정·삭제하는지 먼저 구분합니다.
3. 작업 전에 현재 문제와 완료 조건을 적습니다.
4. 작업이 끝나면 실제 변경 결과와 검증 명령을 기록합니다.
5. 자세한 과정과 초보자 설명은 `docs/development/DEVELOPMENT_LOG.md`에 날짜별로 추가합니다.

---

## 이전 완료 작업 기록

### 기본 정보 (완료 이력)

- 작업 이름: Supabase 개발 전 최소 하네스 보강
- 시작일: 2026-09-09
- 상태: 완료
- 참고 문서: `docs/planning/SUPABASE_ADMIN_PLAN.md`
- 작업 원칙: 현재 상태와 문서를 맞추고 반복 가능한 기본 검사만 추가

### 요청 배경

Supabase와 관리자 기능처럼 여러 단계가 이어지는 개발을 새 채팅에서도 일관되게 진행하려면 최신 상태, 실행 순서, 완료 조건과 자동 검사가 필요합니다. 현재 프로젝트 규모에 필요한 최소 하네스부터 적용합니다.

### 추가할 것

- [x] 단계와 검증 상태를 관리하는 `SUPABASE_EXECUTION_PLAN.md`
- [x] GitHub에서 필수 검사를 실행하는 Actions 워크플로

### 수정할 것

- `AGENTS.md`에 Supabase 기획·실행 계획 연결
- `PROJECT_STATUS.md`의 다음 작업을 스키마 조정부터 시작하도록 갱신
- 누적 개발 로그에 하네스 변경 기록

### 삭제할 것

- 없음

### 하지 않을 것

- DB 스키마와 RLS 변경
- Google 로그인과 관리자 화면 구현
- 기존 MDX 삭제 또는 DB 이전
- GitHub push와 Vercel 배포

### 완료 조건

- `AGENTS.md`에서 Supabase 기획과 실행 계획을 찾을 수 있음
- 현재 상태와 다음 구현 단계가 서로 모순되지 않음
- GitHub Actions가 설치, lint, 타입, 포맷, 빌드를 실행하도록 구성됨
- 애플리케이션 기능과 공개 페이지는 변경되지 않음

### 작업 결과

- `AGENTS.md`를 최신 Supabase 방향과 두 계획 문서의 목차로 갱신했습니다.
- `SUPABASE_EXECUTION_PLAN.md`에 스키마 조정부터 배포까지 단계와 완료 조건을 추가했습니다.
- 현재 DB와 기획의 차이를 해결하기 전에는 Auth 구현으로 넘어가지 않도록 순서를 명시했습니다.
- pull request와 `main` push에서 네 가지 품질 검사를 실행하는 GitHub Actions를 추가했습니다.
- 실제 원격 Actions 결과는 GitHub에 push한 뒤 확인해야 합니다.

---

## 다음 요청에서 복사해 사용할 형식

```md
## 현재 작업

### 기본 정보

- 작업 이름:
- 시작일:
- 상태: 대기 / 진행 중 / 완료 / 보류
- 참고 문서:
- 작업 원칙:

### 요청 배경

### 추가할 것

- [ ]

### 수정할 것

- [ ]

### 삭제할 것

- [ ]

### 하지 않을 것

-

### 완료 조건

-

### 작업 결과

-
```
