# BUILD & LEARN 개발 요청 관리 문서

이 문서는 새로운 개발 요청을 받을 때마다 **같은 파일을 계속 수정하는 단일 작업 요청서**입니다. 완료된 작업의 자세한 기록은 `docs/development/DEVELOPMENT_LOG.md`에 누적합니다.

## 현재 작업

### 기본 정보

- 작업 이름: 중복 조회 제한과 Vercel 방문 통계 연결
- 시작일: 2026-09-22
- 상태: 완료
- 참고 화면: `/log/[slug]`, `/projects/[slug]`, Vercel Analytics 대시보드
- 작업 원칙: 이름·로그인 ID·IP는 저장하지 않고, 같은 브라우저의 반복 새로고침을 줄인다. 관리자 방문 로그는 브라우저가 만든 익명 UUID와 유입 URL만 기록하며, 운영 통계는 Vercel에도 위임한다.

### 요청 배경

현재 상세 페이지는 서버 렌더링마다 조회수를 증가시켜 같은 방문자가 새로고침해도 숫자가 계속 올라갑니다. 또한 관리자 화면의 숫자만으로는 유입·페이지별 방문 같은 운영 통계를 볼 수 없습니다.

### 추가할 것

- [x] 같은 브라우저·같은 글의 24시간 중복 조회 제한
- [x] Vercel Analytics 패키지와 루트 수집 컴포넌트

### 수정할 것

- [x] Log·Project 상세의 서버 렌더링 즉시 조회수 증가를 클라이언트 추적기로 교체

### 삭제할 것

- 없음

### 하지 않을 것

- 방문자 계정·이름·IP 저장
- 자체 관리자 통계 대시보드 구현

### 완료 조건

- 같은 브라우저가 같은 공개 글을 24시간 안에 다시 열면 DB 조회수가 늘지 않음
- 브라우저 저장소가 막혀도 공개 상세는 정상 표시됨
- Vercel 운영 배포에서 Analytics 수집 코드가 포함됨
- `npm run lint`, `npm run type-check`, `npm run format:check`, `npm run build` 통과

### 작업 결과

- `ContentViewTracker`가 공개 Log·Project 상세가 화면에 표시된 뒤에만 조회수를 요청합니다. 같은 브라우저·같은 slug의 마지막 요청 시각을 localStorage에 기록해 24시간 안의 새로고침·재방문 요청을 막습니다. 저장소를 쓸 수 없는 환경에서는 본문을 막지 않고 기존처럼 요청합니다.
- 중복 제한을 통과한 열람만 `content_view_events`에 기록해 `/admin/visits`에서 시간·글·유입 도메인별로 볼 수 있습니다. 이 이벤트는 이름·이메일·IP·로그인 ID가 아닌 브라우저 localStorage의 임의 UUID와 referrer만 보관합니다.
- Server Action은 콘텐츠 종류와 slug 형식을 제한하고, 기존 Supabase RPC가 `publication_status = 'published'` 행만 증가시키는 보호를 그대로 사용합니다. 새 테이블·마이그레이션·RLS 변경은 없습니다.
- 루트 레이아웃에 `@vercel/analytics/next`의 `Analytics`를 연결했습니다. Vercel Dashboard에서 Analytics를 한 번 활성화한 뒤 Production으로 배포해야 실제 통계가 집계됩니다.
- 변경 파일 lint, TypeScript, Prettier 검사와 Next.js 프로덕션 빌드를 통과했습니다. 로컬 개발 서버는 `.next` 잠금 파일 접근 거부로 실행하지 못해 브라우저에서의 반복 새로고침 수동 확인은 배포 후 운영 주소에서 진행합니다.

## 바로 이전 완료 작업

### 기본 정보

- 작업 이름: Medium에서 영감을 받은 Log 상세 읽기 화면
- 시작일: 2026-09-17
- 상태: 완료
- 참고 화면: Medium의 긴 글 상세 화면
- 작업 원칙: Medium을 그대로 복제하지 않고, BUILD & LEARN의 구조와 접근성·다크 모드를 유지한 채 읽기 경험만 차분하게 정리

### 요청 배경

현재 Log 상세는 큰 그라데이션 제목 영역과 소제목별 파랑·보라·초록·노랑 배경으로 구역을 나눕니다. 사용자는 색을 많이 쓰지 않고, 부드러운 본문 글꼴과 여백·굵기 중심으로 중요한 부분만 강조하는 긴 글 화면을 원합니다.

### 추가할 것

- [x] Log 본문에만 사용하는 부드러운 한글 읽기 글꼴
- [x] 발행일과 예상 읽기 시간을 한 줄로 보여주는 메타 정보

### 수정할 것

- [x] Log 상세 본문 폭과 제목·요약·태그의 정보 위계
- [x] 소제목·인용·코드·표·Callout을 무채색 중심으로 단순화
- [x] 모바일과 다크 모드에서도 같은 읽기 흐름 유지

### 삭제할 것

- [x] Log 상세 제목 영역의 색상 그라데이션과 카드 그림자
- [x] 본문 `h2`마다 순환하는 네 가지 강조색

### 하지 않을 것

- Project 상세와 공개 목록 디자인 변경
- Supabase 데이터·스키마·RLS 변경
- Medium의 로고·아이콘·고유 UI 복제

### 완료 조건

- Log 상세가 약 680~720px의 읽기 열과 일관된 무채색 위계로 표시됨
- 데스크톱·모바일·라이트/다크 모드에서 가로 넘침과 대비 문제가 없음
- `npm run lint`, `npm run type-check`, `npm run format:check`, `npm run build` 통과
- 변경 결과가 누적 개발 로그에 기록됨

### 작업 결과

- Log 상세 전체 폭을 820px, 실제 본문 읽기 열을 680px로 제한하고 제목·요약·발행일·예상 읽기 시간·태그 순서로 정보 위계를 정리했습니다.
- 긴 본문과 요약에는 `Noto Serif KR`을 적용하고 제목·메뉴·소제목에는 기존 `Space Grotesk`를 유지했습니다.
- 제목 영역의 그라데이션·카드 그림자와 `h2` 네 가지 순환색을 제거했습니다. 인용·인라인 코드·표·기본 Callout은 무채색으로 바꾸고, warning·success처럼 의미가 있는 상태색만 남겼습니다.
- 데스크톱 브라우저에서 글 영역 820px·본문 680px, 모바일 브라우저에서 본문 17px·줄간격 32.3px로 표시되는 것을 확인했습니다. 두 화면 모두 가로 넘침과 콘솔 오류가 없었고 키보드 본문 바로가기 링크도 정상 표시됐습니다.
- 다크 모드는 새 스타일이 기존 `--background`, `--foreground`, `--muted`, `--line`, 의미색 변수만 참조하도록 구성해 기존 테마 토큰을 그대로 따릅니다.
- `npm run lint`, `npm run type-check`, `npm run build`를 통과했습니다. 최종 문서 반영 후 `npm run format:check`를 재실행합니다.

> 아래는 2026-09-16에 완료한 문서 정합성 작업(다크 모드·댓글 기능)의 상세 기록입니다.

### 기본 정보

- 작업 이름: 다크 모드·댓글 기능 문서 정합성 정리
- 시작일: 2026-09-16
- 상태: 완료
- 참고 문서: `docs/harness/PROJECT_STATUS.md`, `docs/development/DEVELOPMENT_LOG.md`
- 작업 원칙: 코드를 바꾸지 않고, 이미 구현된 기능이 상태·로그 문서에 정확히 반영되도록만 한다

### 요청 배경

`git log` 확인 결과 `PROJECT_STATUS.md`(2026-09-13 하네스 정리 커밋 `ebe0b04` 기준 작성)가 그 이후 커밋 두 개(`aa3e1aa`, `c822d39`)를 반영하지 못하고 있었습니다. 그 결과 이미 구현된 Log 댓글 기능이 "아직 없음" 목록에 남아 있었고, 다크 모드는 상태 문서에 아예 언급이 없었습니다. `DEVELOPMENT_LOG.md`에도 두 커밋의 작업 기록이 없었습니다.

### 추가할 것

- [x] `DEVELOPMENT_LOG.md`에 두 커밋(About 타임라인·Log 댓글, 다크 모드·댓글 버그 수정)에 대한 누적 로그 항목

### 수정할 것

- [x] `PROJECT_STATUS.md`의 "현재 구현 상태"에 다크 모드 추가
- [x] `PROJECT_STATUS.md`의 "Supabase·관리자 기능" 완료 목록에 About 커리어 타임라인, Log 회원 댓글 추가
- [x] `PROJECT_STATUS.md`의 "아직 없음" 목록에서 댓글 제거
- [x] `PROJECT_STATUS.md`의 "다음 작업 후보" 1번에 댓글·다크 모드 운영 검증 항목 추가

### 삭제할 것

- 없음

### 하지 않을 것

- 애플리케이션 코드 변경
- 새 기능 추가
- Vercel 실제 운영 검증(다음 작업 후보로 남겨둠)

### 완료 조건

- `PROJECT_STATUS.md`가 최신 커밋(`c822d39`)까지의 실제 구현 상태를 정확히 설명함
- `DEVELOPMENT_LOG.md`에 두 커밋에 대응하는 기록이 있음
- `npm run format:check` 통과

### 작업 결과

- `PROJECT_STATUS.md`에 다크 모드, About 커리어 타임라인, Log 회원 댓글을 완료 항목으로 반영하고 "아직 없음" 목록을 정정했습니다.
- `DEVELOPMENT_LOG.md`에 두 커밋에 대응하는 로그 2건(2026-09-13 (15), (16))을 추가했습니다. 다만 두 커밋 자체에는 당시의 lint/type-check/build 실행 기록이나 RLS 허용/거부 실제 테스트 기록이 남아 있지 않아, 이번 로그에는 커밋 diff와 메시지에서 확인 가능한 사실만 적고 그 한계를 명시했습니다.
- 코드는 변경하지 않았습니다. `npm run format:check`만 통과를 확인했습니다(문서만 변경했으므로 `docs/harness/QUALITY_GATES.md`의 "문서만 변경한 경우" 기준 적용).

> 아래의 상세 내용은 2026-09-09에 완료된 하네스 보강 작업의 기록입니다. 활성 개발 요청이 아닙니다.

## 사용하는 방법

1. 새 요청을 시작할 때 아래의 `현재 작업`을 새 내용으로 교체합니다.
2. 무엇을 추가·수정·삭제하는지 먼저 구분합니다.
3. 작업 전에 현재 문제와 완료 조건을 적습니다.
4. 작업이 끝나면 실제 변경 결과와 검증 명령을 기록합니다.
5. 자세한 과정과 초보자 설명은 `docs/development/DEVELOPMENT_LOG.md`에 날짜별로 추가합니다.

---

## 이전 완료 작업 기록

### 기본 정보 (완료 이력)

- 작업 이름: Supabase 개발 전 최소 하네스 보강
- 시작일: 2026-09-09
- 상태: 완료
- 참고 문서: `docs/planning/SUPABASE_ADMIN_PLAN.md`
- 작업 원칙: 현재 상태와 문서를 맞추고 반복 가능한 기본 검사만 추가

### 요청 배경

Supabase와 관리자 기능처럼 여러 단계가 이어지는 개발을 새 채팅에서도 일관되게 진행하려면 최신 상태, 실행 순서, 완료 조건과 자동 검사가 필요합니다. 현재 프로젝트 규모에 필요한 최소 하네스부터 적용합니다.

### 추가할 것

- [x] 단계와 검증 상태를 관리하는 `SUPABASE_EXECUTION_PLAN.md`
- [x] GitHub에서 필수 검사를 실행하는 Actions 워크플로

### 수정할 것

- `AGENTS.md`에 Supabase 기획·실행 계획 연결
- `PROJECT_STATUS.md`의 다음 작업을 스키마 조정부터 시작하도록 갱신
- 누적 개발 로그에 하네스 변경 기록

### 삭제할 것

- 없음

### 하지 않을 것

- DB 스키마와 RLS 변경
- Google 로그인과 관리자 화면 구현
- 기존 MDX 삭제 또는 DB 이전
- GitHub push와 Vercel 배포

### 완료 조건

- `AGENTS.md`에서 Supabase 기획과 실행 계획을 찾을 수 있음
- 현재 상태와 다음 구현 단계가 서로 모순되지 않음
- GitHub Actions가 설치, lint, 타입, 포맷, 빌드를 실행하도록 구성됨
- 애플리케이션 기능과 공개 페이지는 변경되지 않음

### 작업 결과

- `AGENTS.md`를 최신 Supabase 방향과 두 계획 문서의 목차로 갱신했습니다.
- `SUPABASE_EXECUTION_PLAN.md`에 스키마 조정부터 배포까지 단계와 완료 조건을 추가했습니다.
- 현재 DB와 기획의 차이를 해결하기 전에는 Auth 구현으로 넘어가지 않도록 순서를 명시했습니다.
- pull request와 `main` push에서 네 가지 품질 검사를 실행하는 GitHub Actions를 추가했습니다.
- 실제 원격 Actions 결과는 GitHub에 push한 뒤 확인해야 합니다.

---

## 다음 요청에서 복사해 사용할 형식

```md
## 현재 작업

### 기본 정보

- 작업 이름:
- 시작일:
- 상태: 대기 / 진행 중 / 완료 / 보류
- 참고 문서:
- 작업 원칙:

### 요청 배경

### 추가할 것

- [ ]

### 수정할 것

- [ ]

### 삭제할 것

- [ ]

### 하지 않을 것

-

### 완료 조건

-

### 작업 결과

-
```
