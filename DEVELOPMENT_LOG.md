# BUILD & LEARN 누적 개발 작업 로그

이 문서는 사이트에 어떤 문제가 있었고, 어디서 시작했으며, 무엇을 추가·수정·삭제했고, 어떻게 검증했는지를 초보자도 따라갈 수 있도록 기록합니다.

## 문서 운영 규칙

- 새 기능이나 수정 작업을 시작할 때 날짜와 작업 이름을 추가합니다.
- 단순히 “수정함”이라고 쓰지 않고 **문제 → 판단 → 변경 → 검증** 순서로 기록합니다.
- 파일 삭제가 없었더라도 `삭제` 항목에 “없음”이라고 명시합니다.
- 아직 실제 정보가 없는 부분은 완료된 기능처럼 표현하지 않고 placeholder라고 표시합니다.
- 현재 작업 요청은 `DEVELOPMENT_REQUEST.md`, 완료된 상세 과정은 이 문서에 남깁니다.

---

## 8차 보완 — Project와 Log의 MDX 색상 범위 분리

### 시작 상태와 문제

Log 상세용으로 만든 파랑·보라·초록·앰버 에디토리얼 스타일이 공용 `.mdx-content` 선택자에 있었습니다. 같은 MDX 렌더러를 쓰는 Project 상세에도 이 색들이 적용되어 흑백과 주황 단일 포인트라는 Project 디자인 원칙이 깨졌습니다.

### 판단

컴포넌트나 콘텐츠를 나누기보다 CSS 선택자 앞에 `.log-detail`을 붙이는 것이 가장 작은 수정입니다. 공용 가독성 스타일은 흑백·주황으로 두고, 여러 색이 필요한 규칙만 Log 상세 내부에서 덮어쓰도록 정리했습니다.

### 수정

- Project의 제목, 목록, 체크박스, 인용, 인라인 코드, 표, Callout, InputBox를 흑백·주황 톤으로 복원
- 네 가지 제목색과 파랑·보라·초록 계열 블록을 `.log-detail` 내부로 제한
- Log의 인라인 코드 색상이 어두운 코드 블록 내부에 적용되지 않도록 예외 규칙 추가

### 삭제

- 없음

### 초보자 설명

CSS 선택자는 스타일을 받을 대상을 정하는 주소와 비슷합니다. `.mdx-content h2`는 모든 MDX 제목을 가리키지만, `.log-detail .mdx-content h2`는 Log 상세 안의 제목만 가리킵니다. 조건을 하나 더 붙여 다른 페이지로 디자인이 퍼지는 것을 막았습니다.

### 검증 결과

- Project 상세 제목은 투명 배경, 검정 글자, 왼쪽 테두리 없음으로 렌더링됐고 목록 표시는 주황색이었습니다.
- Log 상세의 첫 네 제목은 파랑·보라·초록·앰버로 렌더링됐고 Callout과 InputBox 색상도 유지됐습니다.
- Home, Projects, Log, About, Contact에서 가로 넘침과 브라우저 콘솔 오류가 없었습니다.
- `npm run lint`, `npm run type-check`, `npm run format:check`, `npm run build`를 모두 통과했습니다.

---

## 프로젝트 시작 상태

### 어디서 시작했나요?

기존 Next.js 학습 프로젝트는 연습 목적의 초기 상태였고, 실제 포트폴리오로 계속 확장하기에는 목적과 구조가 섞여 있었습니다. 그래서 `C:\Users\tkznf\Desktop\my\next\build-n-learn`에 새 Next.js 프로젝트를 만들고 BUILD & LEARN 포트폴리오를 다시 구성했습니다.

### 처음 정한 방향

- Next.js App Router와 TypeScript 사용
- DB나 관리자 화면 없이 MDX 파일로 콘텐츠 관리
- Projects와 Log를 중심으로 한 개인 개발 아카이브
- 흑백 편집 디자인과 주황색 포인트 컬러
- 결과뿐 아니라 선택 과정과 학습 내용도 기록

---

## 1차 — MVP 구조 구현

### 문제

새 프로젝트에는 기본 Next.js 화면만 있었고 포트폴리오에 필요한 페이지, 콘텐츠 구조, 필터, 상세 화면이 없었습니다.

### 추가

- Home, Projects, Project Detail, Log, Log Detail, About, Contact 페이지
- 공통 Header, Footer, Badge, Button, SectionHeading 컴포넌트
- 프로젝트와 로그의 TypeScript 타입
- `src/content/projects`, `src/content/logs` MDX 콘텐츠 폴더
- 파일을 읽고 공개 여부·검색·카테고리로 필터링하는 데이터 함수
- URL 쿼리스트링 기반 필터와 검색
- sitemap, robots, 404 페이지

### 수정

- create-next-app 기본 화면을 BUILD & LEARN 디자인으로 교체
- 공통 스타일을 `src/styles/globals.css`로 정리

### 삭제

- 기본 Next.js favicon과 초기 글로벌 스타일
- 기본 예제 화면과 링크

### 초보자 설명

MDX는 Markdown 문서 위에 제목, 카테고리, 날짜 같은 데이터를 함께 적을 수 있는 파일입니다. DB 없이도 파일 하나를 추가하면 목록과 상세 페이지를 만들 수 있어 작은 포트폴리오의 첫 단계에 적합합니다.

### 검증

- 전체 라우트 생성 확인
- 공개 콘텐츠만 목록과 상세 페이지에 노출되는지 확인
- 타입 검사, Lint, 프로덕션 빌드 확인

---

## 교차 점검 — 누락 기능과 유지보수 문제 수정

### 발견한 문제

- 카테고리를 선택한 뒤 검색하면 기존 카테고리가 사라짐
- Log 데이터에는 태그가 있지만 화면에 태그 필터가 없음
- 프로젝트 데모의 embed/link/download 타입이 누락됨
- 썸네일 데이터가 있어도 카드에 이미지가 보이지 않음
- Contact가 텍스트뿐이고 입력 폼이 없음
- About에 Career, Tech Stack, Interests, Development Philosophy가 빠짐
- 코드가 한 줄로 과도하게 압축되고 자동 포맷 도구가 없음

### 추가·수정

- 기존 쿼리스트링을 보존하는 검색 방식
- Log 태그 필터 UI
- 프로젝트 데모 타입과 각각 다른 상세 UI
- `next/image` 기반 프로젝트/로그 썸네일
- Contact Form UI와 입력 검증
- About의 전체 소개 섹션
- Prettier 설정과 format 명령
- 초보자용 `LEARNING_GUIDE.md`

### 삭제

- 없음

### 초보자 설명

검색 URL에 `?category=web&q=Next.js`처럼 여러 조건을 함께 적으면 새로고침하거나 링크를 공유해도 같은 결과를 볼 수 있습니다. 검색할 때 기존 조건을 지우지 않고 새 검색어만 합치는 것이 핵심이었습니다.

---

## 2차 — SEO, 이미지, 접근성, 반응형 보완

### 추가·수정

- 기본 Open Graph와 Twitter 카드 메타데이터
- 프로젝트/로그 상세 페이지별 메타데이터
- 기본 OG 이미지와 사이트 아이콘
- 로그 썸네일과 태그 링크
- 키보드 포커스 스타일과 본문 바로가기 링크
- 태블릿 2열 레이아웃과 모바일 1열 레이아웃
- Custom 404

### 삭제

- 없음

### 검증

- Home, Projects, Log, Detail, About, Contact 반응형 확인
- 이미지 깨짐과 가로 스크롤 확인
- 전체 자동 검사 통과

---

## 3차 — 폰트, 디자인 토큰, 예시 콘텐츠 확장

### 문제

시스템 폰트에 의존했고 글자 크기와 간격 값이 선택자마다 흩어져 있었습니다. 콘텐츠 수도 적어 필터와 카드 레이아웃의 실제 모습을 확인하기 어려웠습니다.

### 추가

- Space Grotesk와 Noto Sans KR을 `next/font`로 적용
- 글자 크기, 8px 간격, transition CSS 변수
- 프로젝트 예시 4개와 로그 예시 4개
- 새 콘텐츠용 기하학적 SVG 8개
- 모든 예시 MDX에 교체 예정 PLACEHOLDER 주석

### 수정

- 하드코딩된 타이포그래피·간격을 공통 변수 중심으로 정리
- Home의 Featured가 최신 3개만 표시되도록 확인

### 삭제

- Arial 중심의 시스템 폰트 설정

### 검증

- web/app/game/experiment 카테고리 분기
- embed/link/download 데모 분기
- 공유 MDX 태그에 로그 2개가 표시되는지 확인
- 비공개 프로젝트가 목록과 상세 경로에서 제외되는지 확인

---

## 전체 자가검진 — 기능·접근성·반응형 회귀 테스트

### 발견한 문제

- 검색 후 브라우저 뒤로가기를 하면 URL과 검색 입력값이 달라짐
- 검색 결과가 없을 때 빈 화면만 표시됨
- 목록 페이지의 제목 단계가 `h1` 다음 `h3`으로 건너뜀
- Next.js 16에서 첫 카드 이미지 LCP 로딩 경고 발생

### 수정

- URL 검색어가 달라지면 SearchInput이 다시 만들어지도록 key 적용
- Project와 Log에 결과 없음 상태 추가
- Home 카드 제목은 h3, 목록 카드 제목은 h2가 되도록 선택 가능한 headingLevel 추가
- 폐기된 Image priority 대신 첫 행에 `loading="eager"` 적용
- 로그 날짜에 기계가 읽을 수 있는 `dateTime` 추가

### 삭제

- Next.js 16에서 폐기된 Image `priority` 사용

### 검증

- 모든 공개 페이지와 상세 페이지
- 360, 768, 1024, 1440px 화면
- 필터 조합, 검색, 앞/뒤 이동, 404, Contact Form
- broken image, 중복 ID, 빈 링크, 콘솔 오류, 가로 넘침
- 최종 자동 검사 4종 통과

---

## 4차 — 모바일 Hover와 Hero 점검

### 시작 상태와 문제

프로젝트와 로그 이미지는 CSS 기본값으로 항상 흑백이었고 `:hover`일 때만 컬러가 됐습니다. 스마트폰에는 마우스가 없으므로 모바일 사용자는 컬러 이미지를 볼 방법이 없었습니다. 또한 Hero 제목의 최소 크기가 54px이라 360~430px 화면에서 문장이 부자연스럽게 여러 줄로 나뉠 가능성이 있었습니다.

### 판단

터치할 때마다 컬러를 토글하는 JavaScript를 추가하면 링크 탭 동작과 충돌하고 상태도 복잡해집니다. 모든 기기의 기본을 컬러로 두고, 실제 마우스가 있는 환경에서만 흑백 효과를 추가하는 CSS 방식이 가장 단순하고 안전합니다.

### 추가

- `@media (hover: hover) and (pointer: fine)` 마우스 환경 조건
- 480px 이하 Hero/404용 모바일 글자 크기와 line-height
- 모바일 내비게이션·필터 링크 최소 40px 터치 높이
- 모바일 검색 입력·버튼 최소 44px 높이
- 누적 작업 로그 `DEVELOPMENT_LOG.md`
- 단일 요청 관리 문서 `DEVELOPMENT_REQUEST.md`

### 수정

- 프로젝트/로그 이미지는 기본 컬러, 마우스 환경에서만 흑백→Hover 컬러로 변경
- `LEARNING_GUIDE.md`의 Hover 설명을 실제 동작과 일치하도록 수정

### 삭제

- 모든 기기에 무조건 적용되던 이미지 기본 흑백 필터

### 초보자 설명

CSS 미디어 쿼리는 화면 너비뿐 아니라 입력 장치의 특성도 확인할 수 있습니다. `hover: hover`는 마우스를 올리는 동작이 가능한지, `pointer: fine`은 마우스처럼 정밀한 포인터인지 뜻합니다. 이 조건을 통과한 기기에만 추가 효과를 주면 모바일에서 기능을 잃지 않습니다.

### 검증 결과

- 360·390·430px에서 Home, Projects, embed/link/download Project Detail, Log, Log Detail, About, Contact, 404를 확인했습니다.
- 모든 검사 화면에서 가로 스크롤 0건, 깨진 이미지 0건이었습니다.
- Hero 제목은 360px에서 34.2px, 390px에서 37.05px, 430px에서 40px로 계산됐고 모두 정확히 2줄이었습니다.
- 모바일 내비게이션과 필터 링크는 최소 40px, 검색 입력과 버튼은 최소 44px 이상의 터치 높이를 확보했습니다.
- 1440px 마우스 환경에서 프로젝트 이미지는 기본 `grayscale(1)`, Hover 후 `grayscale(0)`이었고 1.015배 확대와 검은 배경 반전도 유지됐습니다.
- 브라우저 콘솔 오류와 경고가 없었습니다.
- `npm run lint`, `npm run type-check`, `npm run format:check`, `npm run build`를 모두 통과했습니다.

---

## 8차 — 전체 코드 초보자 주석과 클린 코드 점검

### 점검 결과

현재 컴포넌트와 데이터 함수 분리는 프로젝트 규모에 적절해 대규모 리팩터링은 하지 않았습니다. 동작을 바꾸는 것보다 Next.js 자동 동작과 데이터 흐름을 설명하는 것이 학습에 더 도움이 된다고 판단했습니다.

### 추가·수정

- lib·types의 파일 읽기, 변환, 필터, 정렬, URL·OG 선택 주석
- app 페이지의 metadata, 정적 경로, MDX 컴파일, 404 처리 주석
- Client Component, URL 검색 보존, 이미지 로딩, iframe 보안 주석
- CSS의 디자인 변수, 카드, 필터, 상세 본문, 반응형 구역 주석
- `CODE_COMMENT_GUIDE.md`와 `AGENTS.md`의 앞으로의 필수 주석 규칙
- `mdx.ts`의 한 줄 다중 변수 선언을 세 줄로 분리

### 삭제

- 없음

### 초보자 설명

주석은 코드와 같은 말을 반복하기보다 코드만 봐서는 알기 어려운 목적과 이유를 설명해야 합니다. 기능을 바꾸면 관련 주석도 함께 바꿔야 오래된 설명이 잘못된 정보를 주지 않습니다.

### 검증 결과

- Home, Projects 검색 조합, Log, 8차 상세, About, Contact가 정상 렌더링됐습니다.
- 모든 검사 페이지의 가로 넘침과 브라우저 콘솔 오류·경고가 없었습니다.
- Home 최신 Log는 8차→7차→6차 순서였습니다.
- `npm run lint`, `npm run type-check`, `npm run format:check`, `npm run build`를 모두 통과했고 빌드 경고도 없었습니다.

---

## 7차 — 배포 SEO 주소와 Log 상세 색상 보완

### 문제

Vercel의 배포별 주소가 metadata·robots·sitemap에 들어가 고정 서비스 주소와 달랐습니다. SVG 썸네일은 일부 공유 서비스에서 OG 이미지로 표시되지 않을 수 있었고, 날짜가 같은 Log는 실제 작업 순서대로 정렬되지 않았습니다. Log 상세는 흰색과 주황색 중심이라 구역별 의미 구분도 약했습니다.

### 추가·수정

- `VERCEL_PROJECT_PRODUCTION_URL`을 `VERCEL_URL`보다 먼저 사용하는 URL 우선순위
- PNG·JPG·JPEG·WebP만 개별 OG로 허용하고 나머지는 공용 PNG를 쓰는 함수
- 같은 날짜 안의 작업 순서를 나타내는 Log `order`
- 흰색 Log 본문 종이와 파랑·보라·초록·앰버 구역 제목
- 파랑 메모, 보라 인용·정보, 초록 완료, 앰버 주의 색상 체계
- 코드·표·체크박스의 역할별 색상

### 삭제

- 없음

### 초보자 설명

Vercel의 배포 URL은 새 배포마다 바뀔 수 있지만 프로젝트 프로덕션 URL은 계속 유지됩니다. OG와 sitemap처럼 외부 서비스가 저장하는 주소에는 고정 URL을 사용해야 합니다. 색상도 장식으로 무작위 사용하지 않고 같은 의미에 같은 색을 사용하면 긴 문서가 더 읽기 쉬워집니다.

### 검증 결과

- localhost, 임시 배포, 고정 프로덕션, 사용자 지정 URL의 네 단계 우선순위를 확인했습니다.
- 시뮬레이션 빌드의 OG·robots·sitemap이 고정 서비스 주소를 사용했습니다.
- Project와 Log 상세 OG는 모두 PNG였고 SVG 입력은 공용 PNG로 대체됐습니다.
- Home Recent Logs는 7차→6차→5차 순서였습니다.
- 390px와 1440px에서 가로 넘침과 콘솔 오류·경고가 없었습니다.
- `npm run lint`, `npm run type-check`, `npm run format:check`, `npm run build`를 모두 통과했고 빌드 경고도 없었습니다.

---

## 6차 — 기술 블로그형 MDX 글쓰기 시스템

### 시작 상태와 문제

Log 본문은 18px 본문과 `h2`, 목록 정도만 꾸며져 있어 긴 글에서 제목 단계와 코드·검증 결과를 빠르게 구분하기 어려웠습니다. Markdown 체크박스와 표도 전용 문법 지원이 없었고, 반복해서 사용할 정보·안내 상자도 없었습니다.

### 참고와 판단

우아한형제들 기술블로그와 토스 테크에서 큰 제목, 짧은 도입, 단계적인 소제목, 넓은 본문 간격을 참고했습니다. 특정 사이트의 디자인을 복제하지 않고 기존 흑백·주황 디자인을 유지하면서 Notion처럼 의미별 블록을 분리했습니다.

### 추가

- `Callout` 안내 블록과 note·warning·success 상태
- `InputBox` 읽기 전용 정보 블록
- 체크박스와 표를 처리하는 `remark-gfm`
- 제목 2·3·4단계, 인용, 링크, 코드, 표의 본문 스타일
- 블록 사용 예시를 담은 작업 Log 템플릿
- 실제 변경을 기록한 6차 사이트 Log

### 수정

- Log와 Project 상세가 같은 MDX 컴포넌트와 GFM 문법 사용
- 모바일 상세 제목을 최대 40px로 낮추고 한국어 단어 단위 줄바꿈 적용
- 모바일 코드와 표는 블록 내부에서만 가로 스크롤하도록 제한

### 삭제

- 없음

### 초보자 설명

Markdown은 `##`, `- [x]`, 백틱처럼 글의 의미를 표시합니다. CSS는 그 의미를 제목 크기, 체크 UI, 어두운 코드 상자처럼 눈에 보이게 만들고, MDX 컴포넌트는 기본 Markdown에 없는 안내 상자를 추가합니다.

### 검증 결과

- 390px에서 긴 제목은 39px, 단어 단위로 줄바꿈됐고 가로 넘침은 0이었습니다.
- 체크박스 9개, Callout 1개, InputBox 1개, 표 1개가 실제 요소로 렌더링됐습니다.
- 데스크톱 본문 폭은 900px이고 기존 Project 상세도 정상 렌더링됐습니다.
- 브라우저 콘솔 오류·경고가 없었습니다.
- `npm run lint`, `npm run type-check`, `npm run format:check`, `npm run build`가 모두 통과했고 빌드 경고도 없었습니다.

---

## 5차 — 배포 준비와 실제 작업 Log 정리

### 시작 상태와 문제

metadata, robots, sitemap의 기준 주소가 아직 소유하지 않은 `buildnlearn.dev`로 고정되어 있었습니다. 로컬·Vercel 미리보기·실제 도메인은 서로 주소가 다르므로 배포 환경에 따라 주소를 바꿀 방법이 필요했습니다. 모바일 검색 버튼은 폭이 좁을 때 두 글자가 나뉠 수 있었고, 사이트 Log에는 실제 작업이 아닌 placeholder 글 6개도 공개되고 있었습니다.

### 판단

사이트 URL은 여러 파일에 반복해서 적지 않고 공통 모듈 한 곳에서 계산해야 합니다. 사용자 지정 환경변수를 가장 먼저 사용하고, 없으면 Vercel이 제공하는 주소, 그것도 없으면 localhost를 사용하도록 정했습니다. Log는 학습용 예시와 실제 기록을 섞지 않고 실제로 수행한 작업만 공개하기로 했습니다.

### 추가

- 사이트 URL 우선순위를 관리하는 `src/lib/site.ts`
- GitHub 연결부터 환경변수·도메인·배포 후 점검까지 설명한 `DEPLOY.md`
- Node 24.x 실행 환경을 알리는 `package.json`의 `engines`
- 이번 작업을 기록한 5차 MDX Log
- `next.config.ts`에 미래의 외부 이미지 도메인 등록 위치 안내

### 수정

- layout, robots, sitemap이 같은 `SITE_URL`을 사용하도록 통합
- 검색 버튼에 최소 너비, 좌우 여백, 줄바꿈 금지 적용
- 작업 Log 템플릿에 직접 수행하거나 확인한 사실만 적도록 원칙 보강

### 삭제

- 실제 경험을 기록한 글이 아니었던 placeholder Log 6개
- 삭제한 예시 Log에서만 사용하던 SVG 이미지 5개
- 코드에 직접 적혀 있던 미확정 `buildnlearn.dev` 주소

### 초보자 설명

환경변수는 코드를 고치지 않고 실행 장소마다 다른 값을 넣는 설정입니다. 로컬에서는 `localhost`, Vercel에서는 자동으로 만들어진 주소, 나중에는 구매한 도메인을 사용할 수 있습니다. URL을 한 파일에서 정하면 metadata와 검색엔진용 파일이 서로 다른 주소를 가리키는 실수도 줄어듭니다.

### 검증 결과

- `NEXT_PUBLIC_SITE_URL` → `VERCEL_URL` → localhost 우선순위를 각각 확인했습니다.
- 390px에서 검색 버튼은 64×46px이고 `검색`이 한 줄로 표시됐습니다.
- 가로 스크롤은 없었고 metadata의 OG 이미지, robots, sitemap은 모두 localhost 주소를 사용했습니다.
- 공개 Log에는 1~5차와 템플릿 정비 기록까지 실제 작업 Log 6개만 표시됐습니다.
- 브라우저 콘솔 오류·경고가 없었습니다.
- `npm run lint`, `npm run type-check`, `npm run format:check`, `npm run build`를 모두 통과했고 빌드 경고도 없었습니다.

---

## 로그 템플릿 정비 — 사이트에 실제 작업 기록 남기기

### 시작 상태와 문제

루트의 `DEVELOPMENT_LOG.md`에는 개발 과정이 있었지만 실제 사이트의 `src/content/logs/`에는 예시 글 6개만 있었습니다. 예시 글도 하나의 짧은 섹션만 사용해 새로운 글을 작성할 때 따라갈 공통 기준이 없었습니다.

### 판단

누적 개발 문서는 전체 프로젝트 이력을 한곳에서 보는 용도로 유지합니다. 실제 사이트 Log에는 작업마다 MDX 파일 하나를 만들어 태그로 찾을 수 있게 합니다. 두 문서는 같은 내용을 일부 공유하지만 읽는 목적이 다릅니다.

### 추가

- 공식 사이트 작업 로그 템플릿 `WORK_LOG_TEMPLATE.md`
- 1차 MVP, 2차 품질·SEO, 3차 폰트·콘텐츠, 4차 모바일 작업 로그 MDX
- 템플릿에 작업 전 상태, 원인, 추가·수정·삭제, 초보자 설명, 자동·수동 검증, 관련 파일, 남은 점 항목

### 수정

- 기존 placeholder 로그 6개를 `기록한 이유 → 핵심 내용 → 왜 중요할까 → 확인한 내용 → 다음에 할 일` 순서로 통일
- 실제 경험이 없는 글은 PLACEHOLDER 주석과 미완료 체크를 유지
- 썸네일 없는 작업 로그가 목록 앞에 추가되면서 발생한 LCP 경고를 해결하기 위해 첫 번째 이미지가 있는 글을 eager 로딩하도록 수정

### 삭제

- 로그 파일 삭제 없음
- 기존 로그의 제각각인 단일 섹션 구성은 공통 구조로 교체

### 초보자 설명

코드 변경 기록과 사이트에 게시하는 글은 목적이 다를 수 있습니다. 누적 문서는 전체 흐름을 빠르게 찾는 목차이고, MDX 작업 로그는 한 작업을 독립적인 글로 읽고 `Codex` 같은 태그로 모아보는 기록입니다.

### 검증 결과

- 실제 1~4차 작업과 이번 템플릿 정비 작업을 포함해 사이트 Log가 총 11개가 됐습니다.
- `/log?tag=Codex`에 실제 작업 로그 5개가 함께 표시됐습니다.
- 새 작업 로그 상세 페이지에는 표준 섹션 10개가 순서대로 표시됐습니다.
- 기존 `mdx-first` 상세 페이지에는 일반 학습 로그용 공통 섹션 5개가 표시됐습니다.
- 가로 스크롤 0건, 깨진 이미지 0건, 브라우저 콘솔 오류·경고 0건이었습니다.
- `npm run lint`, `npm run type-check`, `npm run format:check`, `npm run build`를 모두 통과했습니다.
